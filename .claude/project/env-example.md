# Environment Variables

Create `.env.local` in the project root.

```env
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/reliefroute
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your_google_maps_key_here